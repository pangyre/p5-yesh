package Yesh::Controller::Article::Test;
use parent qw(Yesh::Test);
use Test::More;

sub create : Tests {
    my $self = shift;
    local $TODO = "Currently unimplemented";
}

sub edit : Tests {
    my $self = shift;
    local $TODO = "Currently unimplemented";
}

sub view : Tests {
    my $self = shift;
    local $TODO = "Currently unimplemented";
}

sub article_permissions : Tests {
    my $self = shift;
    local $TODO = "Currently unimplemented";
}

sub atom : Tests {
    my $self = shift;
    local $TODO = "Currently unimplemented";
}

1;

__END__
